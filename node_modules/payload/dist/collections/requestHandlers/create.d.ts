import { Response, NextFunction } from 'express';
import { PayloadRequest } from '../../express/types';
import { Document } from '../../types';
export declare type CreateResult = {
    message: string;
    doc: Document;
};
export default function create(req: PayloadRequest, res: Response, next: NextFunction): Promise<Response<CreateResult> | void>;
