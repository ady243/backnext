import { Response, NextFunction } from 'express';
import { PayloadRequest } from '../../express/types';
import { Document } from '../../types';
export declare type FindByIDResult = {
    message: string;
    doc: Document;
};
export default function findVersionByID(req: PayloadRequest, res: Response, next: NextFunction): Promise<Response<FindByIDResult> | void>;
