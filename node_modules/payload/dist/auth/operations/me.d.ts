import { PayloadRequest } from '../../express/types';
import { User } from '../types';
export declare type Result = {
    user?: User;
    collection?: string;
    token?: string;
    exp?: number;
};
export declare type Arguments = {
    req: PayloadRequest;
    collectionSlug: string;
};
declare function me({ req, collectionSlug, }: Arguments): Promise<Result>;
export default me;
