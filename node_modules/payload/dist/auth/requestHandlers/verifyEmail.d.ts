import { Response, NextFunction } from 'express';
import { PayloadRequest } from '../../express/types';
declare function verifyEmail(req: PayloadRequest, res: Response, next: NextFunction): Promise<any>;
export default verifyEmail;
