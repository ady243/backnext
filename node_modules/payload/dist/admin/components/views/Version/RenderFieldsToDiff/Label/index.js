import React from 'react';
import './index.scss';
const baseClass = 'field-diff-label';
const Label = ({ children }) => (React.createElement("div", { className: baseClass }, children));
export default Label;
