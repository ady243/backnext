import { Payload } from '..';
import { HookName, FieldAffectingData } from './config/types';
import { Operation } from '../types';
import { PayloadRequest } from '../express/types';
declare type Arguments = {
    data: Record<string, unknown>;
    fullData: Record<string, unknown>;
    originalDoc: Record<string, unknown>;
    field: FieldAffectingData;
    operation: Operation;
    overrideAccess: boolean;
    req: PayloadRequest;
    id: string | number;
    relationshipPopulations: (() => Promise<void>)[];
    depth: number;
    currentDepth: number;
    hook: HookName;
    payload: Payload;
    showHiddenFields: boolean;
};
declare const accessPromise: ({ data, fullData, field, operation, overrideAccess, req, id, relationshipPopulations, depth, currentDepth, hook, payload, showHiddenFields, originalDoc, }: Arguments) => Promise<void>;
export default accessPromise;
