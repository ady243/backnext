import { PayloadRequest } from '../express/types';
import { RelationshipField, UploadField } from './config/types';
import { Payload } from '..';
declare type PromiseArgs = {
    data: Record<string, any>;
    field: RelationshipField | UploadField;
    depth: number;
    currentDepth: number;
    req: PayloadRequest;
    overrideAccess: boolean;
    payload: Payload;
    showHiddenFields: boolean;
};
declare const relationshipPopulationPromise: ({ data, field, depth, currentDepth, req, overrideAccess, payload, showHiddenFields, }: PromiseArgs) => () => Promise<void>;
export default relationshipPopulationPromise;
